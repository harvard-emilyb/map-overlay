services:
  map-overlay:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - uploads:/app/static/uploads
      - saved:/app/static/saved
    restart: unless-stopped

volumes:
  uploads:
  saved:
